<div class="row">
    <div class="col-lg-12 search">
        <% if @search.present? %>
          <% @search.each do |result| %>
            <p>
                <%= link_to "#{result.title}", methodology_path(result) %> by 
                <%= link_to "#{result.user.first_name}", profile_path(result.user.id) %>
            </p>
            <p>
                <%= "#{result.content}" %>
            </p>
          <% end %>
        <% else %>
            Nenhum resultado.
        <% end %>
    </div>
</div>