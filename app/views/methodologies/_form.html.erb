<%= simple_form_for(@methodology) do |f| %>
  <%= f.input :title, label: "Titulo" %>
  <%= f.input :content, label: "Conteúdo", input_html: { rows: 10, cols: 70 }%>
  <%= f.hidden_field :user_id, value: current_user.id %>
  <%= f.submit %>
<% end %>
